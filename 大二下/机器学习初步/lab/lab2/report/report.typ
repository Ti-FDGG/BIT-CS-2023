#import "template.typ": report-body, appendix
#import "@preview/numbly:0.1.0":numbly

#show: doc => report-body(
  class: "07112304",
  student-id: "1120233329",
  author: "陈墨霏",
  header: "机器学习初步实验报告",
  title: "作业2：深度学习基础训练",
  doc
)

#set heading(numbering: numbly(
  "",
  "",
  "{2}.{3}",
  "{2}.{3}.{4}",
  "",
))

== 任务1：用MLP模型对mnist数据集进行分类

从输出结果来看，模型设置符合作业代码内注释要求，与作业要求所给示例保持一致；模型评分与作业要求所给示例输出在一定随机误差范围内接近，符合预期。

#figure(
  image("assets/task1.png", width: 90%),
  caption: "MLP模型对mnist数据集进行分类的结果"
)

== 任务2：用CNN模型对mnist数据集进行分类

从输出结果来看， `loss` 和 `accuracy` 均在合理范围内，与作业要求所给示例输出接近，符合预期。

#figure(
  image("assets/task2.png", width: 90%),
  caption: "CNN模型对mnist数据集进行分类的结果"
)

== 开放性问题

#text(font: "SimHei",)[开放性问题言之有理即可，没有标准答案，每个问题的答案字数需在50-300范围内。]

1. MLP模型中学习率、隐藏层大小、训练轮次对模型性能的影响；

  - 学习率：学习率过大会导致模型不收敛，过小则收敛速度慢。适当的学习率可以加快收敛速度，提高模型性能。
  - 隐藏层大小：隐藏层大小影响模型的表达能力，过小可能导致欠拟合，过大则可能导致过拟合。需要根据数据集的复杂度进行调整。
  - 训练轮次：训练轮次过少可能导致欠拟合，过多则可能导致过拟合。需要通过验证集来选择最佳的训练轮次。

2. 尝试对CNN模型中的激活函数、卷积层参数、池化层参数进行修改，并分析其影响。

  - 激活函数：不同的激活函数会影响模型的非线性表达能力。例如，ReLU函数计算简单且能有效缓解梯度消失问题，而Sigmoid函数可能导致梯度消失，影响模型训练效果。
  - 卷积层参数：卷积核的大小和数量会影响特征提取的能力。较大的卷积核可以捕捉更大的特征，但可能丢失细节；较小的卷积核则更适合捕捉局部特征。
  - 池化层参数：池化层的窗口大小和步幅会影响特征降维的程度。较大的窗口和步幅会导致更多的信息丢失，而较小的窗口和步幅则可能保留更多细节，但计算量会增加。
